# VIBE 生活待办与记事本 · 测试用例文档

> 版本：v1.0
> 日期：2026-08-19
> 依据：需求文档 v1.0 + 技术设计文档 v1.0
> 测试阶段：系统测试（含接口、安全、集成、前端、端到端）

---

## 1. 概述

### 1.1 测试范围

| 模块 | 对应需求/设计 | 是否本期测试 |
|------|--------------|--------------|
| 账号与认证 | FR-15 ~ FR-19、设计第 5 章 | 是 |
| 待办事项 | FR-01 ~ FR-09、FR-13、FR-14 | 是 |
| 记事本 | FR-10、FR-13 | 是 |
| 习惯打卡 | FR-12 | 是 |
| 纪念日 | FR-11 | 是 |
| 提醒与消息 | FR-03、设计第 6 章 | 是 |
| 安全 | 设计第 5 章 | 是 |
| 前端 | 设计第 8 章 | 是 |
| 性能与可靠性 | 设计第 10 章 | 是 |
| 数据库 | 设计第 4 章 | 是 |

### 1.2 本期不测范围

统计报表、数据导入导出、多重提醒、多端同步、主题切换（FR-B1~B5，backlog 未实现）；邮件/短信提醒（未纳入）。

### 1.3 测试环境

1. 前端：Vite dev server（http://localhost:5173）
2. 后端：Gin API（http://localhost:8080）
3. 中间件：MySQL 8.0、Redis 7、RabbitMQ 3.12
4. 浏览器：Chrome 最新版、Edge 最新版
5. 测试账号：`user_a`（密码 `Abc12345`）、`user_b`（密码 `Abc12345`）

### 1.4 测试数据准备

1. 执行数据库迁移脚本，确认 8 张业务表创建成功。
2. 预置 `user_a`、`user_b` 两个账号，密码均为 `Abc12345`。
3. 为 `user_a` 预置：2 条待办（1 条已完成、1 条已逾期）、1 条记事、1 个习惯（含 3 天连续打卡）、1 个纪念日、2 个标签。
4. 启动 api / scheduler / worker 三个进程，确认日志正常、无启动失败。

### 1.5 测试假设（如与预期不符，需用户确认）

- 全天事项的默认提醒时间为当天 09:00。
- 纪念日"提前 N 天提醒"默认在当天 09:00 触发。
- 2 月 29 日纪念日，平年按 2 月 28 日计算。
- 待办转记事后，原待办自动标记为已完成。

---

## 2. 通用约定

- 成功响应：`{"code": 0, "message": "ok", "data": ...}`。
- 失败响应：`code` 非 0，`message` 为可读信息。
- 鉴权失败：`code=1002`，HTTP 401；无权限/资源不存在：`code=1003/1004`，HTTP 403/404。
- 参数错误：`code=1001`，HTTP 400；资源冲突：`code=1005`，HTTP 409；限流：`code=1006`，HTTP 429。
- 时间格式：`YYYY-MM-DD HH:mm:ss`；日期：`YYYY-MM-DD`。
- 优先级：0 高 / 1 中 / 2 低；状态：0 未完成 / 1 已完成。

### 用例优先级定义

| 等级 | 定义 | 通过要求 |
|------|------|----------|
| P0 | 核心链路与安全，阻塞发布 | 100% 通过 |
| P1 | 重要功能与边界场景 | ≥ 95% 通过 |
| P2 | 一般功能与体验细节 | ≥ 90% 通过 |

---

## 3. 账号与认证（FR-15 ~ FR-19）

### 3.1 注册

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-AUTH-001 | 注册成功 | P0 | 数据库无同名用户 | POST `/auth/register`，body：`{"username":"new_user","password":"Abc12345","nickname":"新用户"}` | code=0；可立即用该账号登录；数据库 `password_hash` 为 bcrypt 串且不等于明文 |
| TC-AUTH-002 | 用户名重复 | P0 | 已存在 `user_a` | 以 `username=user_a` 注册 | code=1005，提示用户名已存在；不产生新记录 |
| TC-AUTH-003 | 密码强度不足（过短） | P1 | 无 | password=`Abc123`（6 位） | code=1001，提示至少 8 位 |
| TC-AUTH-004 | 密码无字母或数字 | P1 | 无 | 分别提交 `12345678`、`abcdefgh` | code=1001，提示需同时包含字母和数字 |
| TC-AUTH-005 | 必填字段缺失 | P1 | 无 | 缺 username / password / nickname 任一 | code=1001，字段级错误信息 |
| TC-AUTH-006 | 用户名超长/含非法字符 | P2 | 无 | username 33 位或含空格 | code=1001，格式错误 |
| TC-AUTH-007 | 邮箱重复 | P2 | 已存在邮箱 `a@test.com` 的用户 | 新用户注册使用该邮箱 | code=1005，邮箱已被占用 |

### 3.2 登录

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-AUTH-008 | 登录成功 | P0 | `user_a` 存在 | POST `/auth/login`，正确密码 | code=0；返回 `access_token`、`expires_in=900`、用户信息；响应 `Set-Cookie` 含 HttpOnly 的 `refresh_token` |
| TC-AUTH-009 | 密码错误 | P0 | `user_a` 存在 | 提交错误密码 | code=2001 类认证错误；不返回任何 Token |
| TC-AUTH-010 | 用户不存在与密码错误的提示一致性 | P1 | 无 | 分别用不存在的用户名、错误密码登录 | 两条响应的 message 一致，避免用户名枚举 |
| TC-AUTH-011 | 被禁用用户登录 | P1 | 将 `user_a.status=0` | 正确密码登录 | code=1003，提示账号不可用 |
| TC-AUTH-012 | 登录失败锁定（FR-20 启用时） | P1 | FR-20 已启用 | 同一账号连续失败 5 次后第 6 次用正确密码 | 第 6 次返回 code=1006/锁定提示；15 分钟后恢复可登录 |
| TC-AUTH-013 | 登录成功清除失败计数 | P2 | 已有 3 次失败 | 第 4 次用正确密码登录成功 | 登录成功；失败计数清零；再错误 5 次才触发锁定 |

### 3.3 鉴权与用户信息

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-AUTH-014 | 有效 Token 获取用户信息 | P0 | 已登录 | GET `/auth/me`，携带 Bearer Token | code=0；返回当前用户；不包含 `password_hash` |
| TC-AUTH-015 | 无 Token 访问受保护接口 | P0 | 未登录 | GET `/auth/me` 不带 Authorization | HTTP 401，code=1002 |
| TC-AUTH-016 | 过期 Access Token | P0 | 构造已过期的合法 Token（或等待 15 分钟） | GET `/auth/me` | HTTP 401，code=1002；前端应触发刷新流程 |
| TC-AUTH-017 | 篡改 Token | P0 | 修改合法 Token 的任意字符 | GET `/auth/me` | HTTP 401，签名校验失败 |
| TC-AUTH-018 | 伪造 Token（错误密钥签发） | P0 | 用测试密钥之外的自定义密钥签发 JWT | GET `/auth/me` | HTTP 401 |
| TC-AUTH-019 | Refresh Token 冒充 Access Token | P1 | 已登录，取得 refresh_token | 将 refresh_token 放入 Authorization 调用业务接口 | HTTP 401，`typ` 不匹配 |
| TC-AUTH-020 | 用户禁用后已签发 Token 失效 | P1 | `user_a` 已登录 | 将 `status=0`，用原有 Access Token 访问 | HTTP 401/403，用户状态校验拦截 |

### 3.4 刷新与登出

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-AUTH-021 | 刷新成功并轮换 | P0 | 已登录 | POST `/auth/refresh`（携带 refresh Cookie） | code=0；返回新 Access Token + 新 Refresh Cookie；旧 refresh_token 立即失效 |
| TC-AUTH-022 | 旧 Refresh Token 重放 | P0 | 已执行一次刷新 | 用第一次的旧 refresh_token 再次刷新 | 刷新失败；该用户全部会话被吊销（含最新 Token 也失效） |
| TC-AUTH-023 | 登出后 Token 失效 | P0 | 已登录 | POST `/auth/logout` 后，用原 Access Token 调用 `/auth/me` | HTTP 401，黑名单生效 |
| TC-AUTH-024 | 登出后 Refresh 失效 | P0 | 已登录 | 登出后用原 refresh Cookie 调用 `/auth/refresh` | code=1002，白名单已删除 |
| TC-AUTH-025 | 无 Cookie 刷新 | P1 | 未登录 | POST `/auth/refresh` | HTTP 401，缺少凭据 |
| TC-AUTH-026 | 刷新 Cookie 路径限制 | P2 | 已登录 | 检查 Set-Cookie 属性 | 含 `HttpOnly; Secure; SameSite=Strict; Path=/api/v1/auth` |

---

## 4. 待办模块（FR-01 ~ FR-09、FR-13、FR-14）

### 4.1 新建

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-TODO-001 | 最小信息新建 | P0 | `user_a` 已登录 | POST `/todos`：仅 title + event_date | code=0；列表可见；默认优先级中、无提醒、无重复 |
| TC-TODO-002 | 完整信息新建（时间段+提醒+标签） | P0 | 已登录，有标签 id=1,2 | title/event_date/start_time/end_time/priority/category/tags/reminder_enabled/remind_offset_minutes | code=0；详情字段与提交一致；`reminder_tasks` 生成一行，`remind_at = start_time - 10 分钟`；todo_tags 写入 2 行 |
| TC-TODO-003 | 标题必填 | P1 | 已登录 | 提交空 title 或纯空格 title | code=1001 |
| TC-TODO-004 | 日期格式错误 | P1 | 已登录 | event_date=`2026/08/20` | code=1001 |
| TC-TODO-005 | 结束时间不晚于开始时间 | P0 | 已登录 | start_time=`15:30`、end_time=`14:00` | code=1001；前后端均拦截 |
| TC-TODO-006 | 全天事项带具体时间冲突 | P2 | 已登录 | is_all_day=true 且填写 start_time/end_time | code=1001 |
| TC-TODO-007 | 非法优先级/分类枚举 | P1 | 已登录 | priority=9 或 category=`unknown` | code=1001 |
| TC-TODO-008 | 引用不存在的标签 | P1 | 已登录 | tags=[99999] | code=1004；不写入任何待办数据（事务回滚） |
| TC-TODO-009 | 引用他人标签 | P1 | user_b 已登录 | tags=[user_a 的 tag_id] | code=1004，禁止跨用户引用 |
| TC-TODO-010 | 无标签重复创建同名 | P2 | 已登录 | 连续创建两条相同 title 待办 | 均创建成功（不强制唯一），各自独立 |

### 4.2 查询与筛选

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-TODO-011 | 今日视图 | P0 | 预置今天、明天、逾期各 1 条 | GET `/todos?view=today` | 返回今天的未完成事项 + 逾期事项（标记 overdue）；不包含明天事项 |
| TC-TODO-012 | 状态筛选 | P1 | 预置已完成/未完成各 1 条 | GET `/todos?status=1` | 仅返回已完成 |
| TC-TODO-013 | 日期区间筛选 | P1 | 预置 8/19、8/20 事项 | GET `/todos?start_date=2026-08-19&end_date=2026-08-20` | 两条都返回；超出区间的排除 |
| TC-TODO-014 | 分类+标签组合筛选 | P1 | 预置 work+tag1、life+tag2 | GET `/todos?category=work&tag_ids=1` | 仅返回同时满足分类和标签的事项 |
| TC-TODO-015 | 关键词搜索 | P1 | 预置标题含"开会"事项 | GET `/todos?keyword=开会` | 返回匹配事项；关键词大小写不敏感 |
| TC-TODO-016 | 优先级排序 | P1 | 预置高/中/低各 1 条 | GET `/todos?sort_by=priority&order=asc` | 顺序为 高→中→低 |
| TC-TODO-017 | 分页边界 | P1 | 预置 25 条待办 | page=1&page_size=20；page=2 | 第一页 20 条 total=25；第二页 5 条；page_size>100 被截断为 100 |
| TC-TODO-018 | 日历区间查询 | P1 | 预置本月多条 | GET `/todos/calendar?start_date=2026-08-01&end_date=2026-08-31` | 返回区间内全部事项（含已完成），供日历渲染 |

### 4.3 编辑、完成、删除

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-TODO-019 | 编辑基础字段 | P0 | 已有待办 id=1001 | PUT `/todos/1001` 修改 title/时间 | code=0；详情为更新后数据；`updated_at` 变化 |
| TC-TODO-020 | 编辑时间联动提醒任务 | P0 | 待办已启用提醒 | 修改 start_time 提前 1 小时 | `reminder_tasks.remind_at` 同步重算为新的提前 10 分钟时刻 |
| TC-TODO-021 | 关闭提醒联动 | P1 | 待办已启用提醒 | 编辑 reminder_enabled=false | `reminder_tasks` 中对应任务被移除；不再触发提醒 |
| TC-TODO-022 | 标记完成 | P0 | 未完成待办 | PATCH `/todos/1001/status`，status=1 | code=0；`completed_at` 写入；已完成列表可见；今日视图不再显示 |
| TC-TODO-023 | 恢复未完成 | P0 | 已完成待办 | PATCH status=0 | code=0；`completed_at` 清空；回到未完成列表 |
| TC-TODO-024 | 非法状态值 | P1 | 已有待办 | PATCH status=5 | code=1001 |
| TC-TODO-025 | 删除待办 | P0 | 已有待办 | DELETE `/todos/1001` | code=0；列表查询不到；数据库记录 deleted_at 非空（软删除）；关联提醒任务被移除 |
| TC-TODO-026 | 删除不存在待办 | P1 | 无 | DELETE `/todos/99999` | code=1004 |
| TC-TODO-027 | 编辑他人待办 | P0 | user_b 已登录 | PUT `/todos/{user_a 的 id}` | code=1004；数据未被修改 |
| TC-TODO-028 | 删除他人待办 | P0 | user_b 已登录 | DELETE `/todos/{user_a 的 id}` | code=1004；数据仍在 |

### 4.4 重复事项

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-TODO-029 | 每天重复生成实例 | P1 | 创建 recurrence_type=1、event_date=8/19 | 查询 8/19~8/25 事项 | 每天 1 条实例，内容继承，parent_id 指向根待办 |
| TC-TODO-030 | 每周重复（每周一） | P1 | 创建 recurrence_type=2、rule 指定周一 | 查询 8/17（周一）~8/31 | 仅 8/17、8/24、8/31 出现 |
| TC-TODO-031 | 每月重复跨月 | P1 | 创建 recurrence_type=3、event_date=8/20 | 查询 8/20~9/20 | 8/20 与 9/20 各出现 1 条 |
| TC-TODO-032 | 单独完成某次实例 | P1 | 每周一重复事项已生成 3 条 | 完成其中 8/24 实例 | 8/24 显示已完成；8/17、8/31 仍未完成；后续日期仍正常生成 |
| TC-TODO-033 | 删除根待办后不再生成 | P2 | 重复待办存在 | 删除根待办（parent_id=NULL 的那条） | 已生成实例保留，但不再生成新实例 |
| TC-TODO-034 | 每月重复 31 日边界 | P2 | 创建 recurrence_type=3、event_date=1/31 | 查询 2 月实例 | 2 月按当月最后一天（2/28）生成，3 月回到 3/31（规则需在设计中明确定义并实现一致） |

### 4.5 待办转记事与逾期

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-TODO-035 | 待办转记事成功 | P0 | 已有未完成待办 | POST `/todos/{id}/convert-to-note` | code=0；生成 note 且 `source_todo_id` 指向原待办；原待办自动变为已完成；记事内容包含原 title/description |
| TC-TODO-036 | 重复转记事 | P1 | 已转过的待办 | 再次转换 | code=1005，提示已转换 |
| TC-TODO-037 | 逾期事项标记 | P0 | 预置 event_date=昨天、status=0 | GET `/todos`（含 overdue 标记） | 该条 `overdue=true` 且列表置顶/醒目标识 |
| TC-TODO-038 | 逾期后完成不再标记 | P1 | 逾期事项存在 | 标记完成后查询 | overdue 标记消失 |
| TC-TODO-039 | 今天未完成不标记逾期 | P1 | event_date=今天、未完成 | 查询 | overdue=false |

---

## 5. 记事模块（FR-10、FR-13）

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-NOTE-001 | 新建记事成功 | P0 | 已登录 | POST `/notes`：title+content | code=0；列表与详情可见 |
| TC-NOTE-002 | 标题必填校验 | P1 | 已登录 | title 为空或纯空格 | code=1001 |
| TC-NOTE-003 | 内容超长 | P2 | 已登录 | content 超过字段上限（按实现，如 65535 字节） | 截断或 1001 报错，行为与文档一致 |
| TC-NOTE-004 | 编辑记事 | P1 | 已有记事 | PUT `/notes/{id}` 修改标题 | code=0；内容更新 |
| TC-NOTE-005 | 删除记事 | P1 | 已有记事 | DELETE `/notes/{id}` | code=0；列表不可见；软删除 |
| TC-NOTE-006 | 查看他人记事 | P0 | user_b 已登录 | GET `/notes/{user_a 的 id}` | code=1004 |
| TC-NOTE-007 | 转来记事显示来源 | P2 | 完成 TC-TODO-035 | GET `/notes/{id}` | 返回 `source_todo_id`，前端展示"来自待办"标签 |
| TC-NOTE-008 | 删除来源待办不影响记事 | P2 | 存在转来记事 | 删除原待办后查看记事 | 记事完整保留（冗余存储） |

---

## 6. 习惯打卡（FR-12）

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-HABIT-001 | 新建习惯 | P0 | 已登录 | POST `/habits`：name=喝水 | code=0；列表可见，连续天数为 0 |
| TC-HABIT-002 | 当天打卡成功 | P0 | 已有习惯 | POST `/habits/{id}/checkin` | code=0；连续天数=1 |
| TC-HABIT-003 | 同日重复打卡 | P0 | 今天已打卡 | 再次 checkin | code=1005（唯一约束）；连续天数不变化 |
| TC-HABIT-004 | 连续打卡累计 | P1 | 昨天、前天已打卡 | 今天打卡 | 连续天数=3 |
| TC-HABIT-005 | 中断后重新开始 | P1 | 已有 3 天连续，昨天漏打 | 今天打卡 | 连续天数重置为 1 |
| TC-HABIT-006 | 取消当天打卡 | P1 | 今天已打卡 | DELETE `/habits/{id}/checkin/{today}` | code=0；连续天数相应减少或重置 |
| TC-HABIT-007 | 未来日期打卡 | P1 | 已有习惯 | 提交明天日期打卡 | code=1001，禁止未来打卡 |
| TC-HABIT-008 | 跨月连续计算 | P1 | 7/31、8/1 已打卡 | 8/2 打卡 | 连续天数=3，跨月不中断 |
| TC-HABIT-009 | 他人习惯打卡 | P0 | user_b 已登录 | POST `/habits/{user_a 的 id}/checkin` | code=1004 |
| TC-HABIT-010 | 习惯删除后记录保留 | P2 | 习惯有 10 条打卡记录 | DELETE `/habits/{id}` | 习惯不可见；打卡记录行保留（冗余设计） |

---

## 7. 纪念日（FR-11）

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-ANN-001 | 新建纪念日成功 | P0 | 已登录 | POST `/anniversaries`：name=结婚纪念日、date=今天+30天 | code=0；倒计时 30 天 |
| TC-ANN-002 | 今天即纪念日 | P1 | 已有 date=今天的纪念日 | GET 列表 | 倒计时 0 天，标记"就是今天" |
| TC-ANN-003 | 已过日期按明年计算 | P1 | date=昨天、repeat_yearly=1 | GET 列表 | 倒计时=364 天（按明年同日） |
| TC-ANN-004 | 不重复的纪念日过期 | P1 | repeat_yearly=0、date=昨天 | GET 列表 | 显示"已过"，不再生成倒计时 |
| TC-ANN-005 | 闰年 2/29 边界 | P2 | date=2028-02-29、repeat_yearly=1 | 在 2027-02-28 查询 | 平年按 2/28 显示倒计时（按测试假设） |
| TC-ANN-006 | 编辑纪念日 | P1 | 已有纪念日 | 修改日期 | 倒计时重算；提醒任务联动更新 |
| TC-ANN-007 | 删除纪念日 | P1 | 已有纪念日 | DELETE | code=0；不再显示；提醒任务移除 |
| TC-ANN-008 | 他人纪念日越权 | P0 | user_b 已登录 | GET/PUT/DELETE user_a 的纪念日 | code=1004 |
| TC-ANN-009 | 名称/日期必填 | P1 | 已登录 | 缺 name 或缺 date | code=1001 |

---

## 8. 提醒与消息链路（FR-03、设计第 6 章）

### 8.1 任务生成与联动

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-REM-001 | 待办提醒任务生成 | P0 | 已登录 | 创建待办：start=14:00、offset=10 分钟 | `reminder_tasks` 新增 1 行：remind_at=13:50、status=0 |
| TC-REM-002 | 全天事项默认提醒时间 | P1 | 已登录 | 创建全天待办并启用提醒 | remind_at=当天 09:00（按测试假设） |
| TC-REM-003 | 纪念日提前提醒 | P1 | 已登录 | 创建纪念日：date=8/25、remind_days_before=1 | remind_at=8/24 09:00 |
| TC-REM-004 | 不启用提醒不生成任务 | P1 | 已登录 | reminder_enabled=false | `reminder_tasks` 无记录 |
| TC-REM-005 | 编辑待办重算提醒 | P0 | 已有提醒任务 | 将 start_time 改为 16:00 | remind_at 更新为 15:50；旧任务不残留 |
| TC-REM-006 | 删除待办取消提醒 | P0 | 已有提醒任务 | DELETE 待办 | 对应 reminder_tasks 行被移除 |
| TC-REM-007 | 重复事项每次实例生成提醒 | P1 | 创建每天重复+提醒 | 检查后续 3 天 | 每天 1 条提醒任务，时刻正确 |

### 8.2 调度与投递

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-REM-008 | 到期任务被调度发布 | P0 | 任务 remind_at=now-10s、status=0 | 等待 ≤60 秒 | 消息进入 `vibe.reminder.queue`；任务 status=1 |
| TC-REM-009 | 未到期任务不发布 | P1 | 任务 remind_at=now+30 分钟 | 等待 60 秒 | 队列无该消息；status 仍为 0 |
| TC-REM-010 | 消费者送达并推送 SSE | P0 | 浏览器已连接 SSE；任务到期 | 等待投递 | 浏览器收到 `reminder` 事件；内容与消息体一致；任务 status=2、sent_at 写入 |
| TC-REM-011 | 多用户隔离推送 | P0 | user_a、user_b 各开一个 SSE 连接；仅 user_a 有到期任务 | 触发投递 | 只有 user_a 的连接收到事件 |
| TC-REM-012 | 消息重复投递不重复送达 | P0 | 构造同一 message_id 投递两次 | 手动 requeue/重发 | 仅推送 1 次；`reminder:done:{id}` 去重生效 |
| TC-REM-013 | 消费前崩溃不丢消息 | P1 | 关闭 worker，任务已发布 | 等待后重启 worker | 消息重新投递并最终送达（at-least-once + 幂等） |
| TC-REM-014 | 消费失败重试 | P1 | 构造消费者处理抛错 | 观察队列 | 自动重试至多 3 次；3 次后进入死信队列，任务 status=3 |
| TC-REM-015 | 失败任务补偿 | P1 | 存在 status=3、fail_count<5 的任务 | 等待下一轮补偿扫描 | 任务重新入队并尝试送达；fail_count 递增 |
| TC-REM-016 | 调度器分布式锁 | P1 | 启动两个 scheduler 实例 | 观察日志/DB | 同一时刻仅一个实例执行扫描；锁 TTL 55 秒 |
| TC-REM-017 | SSE 未鉴权拒绝 | P0 | 未登录 | 连接 GET `/events` 无 Token | HTTP 401，不建立事件流 |
| TC-REM-018 | SSE 断线重连 | P1 | 已连接 SSE | 断开网络 10 秒后恢复 | 前端按退避策略自动重连，重连后事件继续到达 |

### 8.3 提醒中心

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-REM-019 | 提醒中心记录 | P1 | 已送达 1 条提醒 | GET `/notifications` | 返回该条记录，未读 |
| TC-REM-020 | 标记已读 | P2 | 已有未读提醒 | PATCH `/notifications/{id}/read` | 已读状态更新；未读计数减 1 |
| TC-REM-021 | 通知授权拒绝仍站内可见 | P1 | 浏览器拒绝 Notification 授权 | 收到提醒 | 浏览器不弹系统通知；提醒中心与页面内卡片正常展示 |

---

## 9. 安全测试（设计第 5 章）

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-SEC-001 | SQL 注入尝试 | P0 | 已登录 | title=`' OR 1=1 --`、keyword=`' OR 1=1 --` | 作为普通字符串处理，无额外数据泄露，无 SQL 报错 |
| TC-SEC-002 | XSS 注入 | P0 | 已登录 | title=`<script>alert(1)</script>` | 后端正常存储；前端渲染为纯文本，不执行脚本 |
| TC-SEC-003 | 响应不含敏感字段 | P0 | 已登录 | 检查 `/auth/me`、待办等接口响应 | 不含 password_hash、refresh_token 明文、解密后 DB 密码 |
| TC-SEC-004 | 横向越权批量遍历 | P0 | user_b 已登录 | 遍历 user_a 的 todo/note/habit/anniversary id | 全部返回 1004；无数据泄露 |
| TC-SEC-005 | 登录接口限流 | P0 | 无 | 同一 IP 高频调用登录（≥ 阈值） | code=1006/429；限制窗口内不再处理 |
| TC-SEC-006 | CORS 非法来源 | P1 | 已登录 | 从 `https://evil.example.com` 发起跨域请求 | 响应无 ACAO 头或拒绝；浏览器拦截 |
| TC-SEC-007 | CORS 合法来源 | P1 | 已登录 | 从 `http://localhost:5173` 发起 | 预检通过，业务正常 |
| TC-SEC-008 | 缺失密钥启动失败 | P0 | 环境无 `VIBE_DB_KEY` | 启动 API 服务 | 进程启动失败，日志提示密钥缺失；不建立数据库连接 |
| TC-SEC-009 | 错误密钥启动失败 | P0 | `VIBE_DB_KEY` 为错误值 | 启动 API 服务 | AES-GCM 解密失败，进程退出 |
| TC-SEC-010 | 密文被篡改 | P1 | 修改 config 密文任意字符 | 启动 API 服务 | GCM 校验失败，进程退出 |
| TC-SEC-011 | 配置无明文密码 | P0 | 检查仓库与运行配置 | 搜索 config/代码中的 MySQL 明文密码 | 无明文；`.gitignore` 覆盖 config.yaml 与 .env |
| TC-SEC-012 | 密码策略强制 | P1 | 无 | 注册弱密码组合（8 位纯数字等） | 全部被拒绝 |
| TC-SEC-013 | 日志脱敏 | P1 | 已登录并发起请求 | 检查日志 | 无 Token、密码、解密后的 DB 密码；有 request_id/user_id |
| TC-SEC-014 | Token 黑名单生效 | P1 | 已登录 | 登出后用原 Access Token 调用接口 | HTTP 401 |
| TC-SEC-015 | 刷新轮换重放全端吊销 | P0 | 已登录 | 刷新一次后用旧 refresh token 再刷新 | 刷新失败且最新会话也失效（重新登录后方可继续） |

---

## 10. 前端测试（设计第 8 章）

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-FE-001 | 未登录跳转登录页 | P0 | 清空存储 | 直接访问 `/today` | 跳转 `/login`，带回跳参数 |
| TC-FE-002 | 登录后回跳 | P0 | 未登录 | 访问 `/today` → 登录成功 | 自动回到 `/today` |
| TC-FE-003 | 静默刷新续期 | P0 | Access Token 即将过期 | 刷新页面访问受保护路由 | 自动调用 refresh 成功，页面正常进入，无登录页闪现 |
| TC-FE-004 | 刷新失败登出 | P0 | Refresh Cookie 已失效 | 刷新页面 | 跳转登录页，提示会话过期 |
| TC-FE-005 | 并发 401 只刷新一次 | P0 | Token 已过期 | 同时发起 3 个请求 | 仅 1 次 `/auth/refresh`；3 个请求全部重试成功 |
| TC-FE-006 | 表单前端校验 | P1 | 登录态 | 填写 end_time≤start_time、弱密码 | 前端拦截提示，不发请求 |
| TC-FE-007 | SSE 自动重连 | P1 | 已登录 | 网络中断 15 秒后恢复 | 连接按退避策略恢复；提醒中心数据不丢 |
| TC-FE-008 | 通知授权交互 | P1 | 已登录 | 首次收到提醒 | 弹出授权请求；拒绝后站内仍可见 |
| TC-FE-009 | 倒计时展示正确 | P1 | 存在纪念日 | 打开纪念日页 | 显示"距离 X 天/就是今天/已过"与后端计算一致 |
| TC-FE-010 | 逾期醒目展示 | P1 | 存在逾期事项 | 打开今日视图 | 逾期条目置顶/红标，符合设计 |
| TC-FE-011 | Token 不落本地存储 | P0 | 登录后 | 检查 localStorage/sessionStorage | 无 access_token、refresh_token；仅有非敏感 UI 状态 |
| TC-FE-012 | 深链接与路由回退 | P2 | 已登录 | 直接输入 `/calendar` 与无效路径 | 有效路由正常渲染；无效路径进入 404 页 |

---

## 11. 性能与可靠性（设计第 10 章）

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-PERF-001 | 常规接口响应时间 | P1 | 预置 500 条待办 | 压测 GET `/todos`、GET `/todos/calendar` | P95 < 300ms |
| TC-PERF-002 | 今日视图加载 | P1 | 预置当天 50 条 | 压测 today 查询 | P95 < 500ms |
| TC-PERF-003 | 大批量提醒投递 | P1 | 生成 1000 条同一分钟到期任务 | 启动投递并观察 | 全部在 60 秒内送达；无重复、无遗漏（status=2 计数=1000） |
| TC-PERF-004 | 分页深翻页 | P2 | 500 条待办 | 访问 page=25 | 正常返回，无超时；确认走索引 |
| TC-PERF-005 | MySQL 连接失败启动 | P0 | 停止 MySQL | 启动 API | 启动失败或进入失败重试并告警，不静默降级 |
| TC-PERF-006 | Redis 运行时中断 | P1 | 服务运行中 | 停止 Redis | 相关功能（刷新/限流/去重）返回明确错误并告警；恢复后自动重连 |
| TC-PERF-007 | RabbitMQ 中断后恢复 | P1 | worker 运行中 | 停止 RabbitMQ 60 秒再启动 | 连接恢复；积压消息继续消费；提醒最终送达 |
| TC-PERF-008 | 调度器多实例互斥 | P1 | 启动两个 scheduler | 观察扫描日志 | 单次扫描仅一个实例执行（锁生效） |

---

## 12. 数据库测试（设计第 4 章）

| 编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|------|----------|--------|----------|----------|----------|
| TC-DB-001 | 迁移脚本可重复执行 | P1 | 全新库 | 执行迁移两次 | 第二次为幂等 no-op，无报错 |
| TC-DB-002 | 软删除过滤 | P1 | 删除 1 条待办 | 查询 todos 表与业务接口 | 业务接口不返回；表内记录 deleted_at 非空 |
| TC-DB-003 | 打卡唯一约束 | P1 | 今日已打卡 | 直接 SQL 插入同日记录 | 唯一键冲突报错 |
| TC-DB-004 | 索引生效验证 | P2 | 预置数据 | EXPLAIN 关键查询（user+date、user+status） | 使用 idx_user_date / idx_user_status，无全表扫描 |
| TC-DB-005 | 备份与恢复 | P1 | 预置全量数据 | mysqldump → 清库 → 恢复 | 数据完整恢复，业务查询一致 |
| TC-DB-006 | 字符集正确性 | P2 | 已登录 | 创建含 emoji 与中文的待办/记事 | 正常存取，无乱码（utf8mb4） |

---

## 13. 端到端场景测试

### 13.1 新用户完整旅程（P0）

| 编号 | 用例名称 | 测试步骤 | 预期结果 |
|------|----------|----------|----------|
| TC-E2E-001 | 注册到提醒全流程 | ① 注册新账号 → ② 登录进入今日视图 → ③ 新建 14:00 待办（提前 10 分钟提醒）→ ④ 等待提醒触发 → ⑤ 收到浏览器通知与站内提醒 → ⑥ 标记完成 → ⑦ 转记事 → ⑧ 打卡喝水 → ⑨ 建纪念日 | 每一步无报错；提醒在 13:50 送达；完成项进入已完成；记事含来源；打卡显示连续 1 天；纪念日倒计时正确 |

### 13.2 日常使用场景（P1）

| 编号 | 用例名称 | 测试步骤 | 预期结果 |
|------|----------|----------|----------|
| TC-E2E-002 | 周一早晨规划 | ① 打开今日视图 → ② 查看逾期（昨天未完成）→ ③ 将逾期事项改到今天 → ④ 添加"每周一例会"重复待办 → ⑤ 查看本周日历 | 逾期醒目；修改后不再逾期；重复事项在下周一自动出现；日历显示正确 |
| TC-E2E-003 | 跨设备会话续期 | ① 登录 → ② 关闭浏览器 10 分钟 → ③ 重新打开访问 | 静默刷新成功，无需重新登录（Refresh 7 天内） |
| TC-E2E-004 | 越权场景回归 | ① 登录 user_b → ② 尝试访问 user_a 的待办/记事/打卡/纪念日 → ③ 尝试用 user_a 的标签 | 全部被拒绝，无数据泄露 |
| TC-E2E-005 | 提醒断网恢复 | ① 登录并打开页面 → ② 断网 30 秒 → ③ 恢复网络 → ④ 触发一条到期提醒 | SSE 重连成功；提醒不丢，最终在提醒中心可见 |

---

## 14. 缺陷等级与通过标准

| 缺陷等级 | 定义 | 处理 |
|----------|------|------|
| 致命（S0） | 数据丢失、账号可被绕过、提醒系统完全不可用 | 阻塞发布，必须修复 |
| 严重（S1） | 核心功能不可用、越权访问、重复提醒 | 发布前修复 |
| 一般（S2） | 非核心功能异常、边界场景错误 | 按迭代计划修复 |
| 轻微（S3） | 文案、样式、体验细节 | 收集优化 |

### 发布通过标准

- P0 用例 100% 通过，S0/S1 缺陷清零。
- P1 用例通过率 ≥ 95%，S2 缺陷 ≤ 10 个且无数据风险。
- P2 用例通过率 ≥ 90%。
- 安全用例（第 9 章）全部通过。

---

## 15. 回归测试范围

以下变更必须触发对应模块回归：

- 认证/中间件变更 → 第 3、9 章全部用例。
- 待办模型变更 → 第 4 章全部 + 提醒联动（TC-REM-001~007）。
- 提醒调度/队列变更 → 第 8 章全部 + TC-PERF-003/007/008。
- 前端请求封装变更 → TC-FE-005、TC-FE-003/004。
- 数据库迁移变更 → TC-DB-001/002/003 + 全量功能冒烟。
- 每次发布前执行第 13 章端到端场景（TC-E2E-001~005）。
